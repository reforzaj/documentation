``` mermaid
sequenceDiagram
    autonumber
    title: Create VA - Open Multiple Use (Open Static)
    participant a as Merchant
    participant b as Open API
    participant c as BE Portal
    a->>b : Request create VA - Open Static
    note left of a : VA generated by Merchant [4 BIN + 4 MID + 8 free]
    b->>c : Request create VA - Open Static
    c->>c : VA Validation ? Is Va Unique ?
    alt VA is Unique
    c-->>b : Return VA creation number
    note right of c : Expired date set to be null
    b-->>a : Return VA creation number
    else VA is not Unique
    c-->>b : Return VA creation error response [VA already been used]
    b-->>a : Return VA creation error response [VA already been used]
    end
```
